# Uncomment and set if the backend isn't reachable at <same-host>:8000/ws
# (e.g. accessing the dashboard from a different machine on the LAN, or the
# Pi is behind a different hostname/port).
# VITE_WS_URL=ws://raspberrypi.local:8000/ws
